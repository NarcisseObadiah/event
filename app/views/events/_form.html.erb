
<%= form_for @event do |f| %>
<%= render "devise/shared/error_messages", resource: resource %>

<h2>Info Basic</h2>
<p>Nommez votre événement et dites aux participants pourquoi ils devraient venir. Ajoutez des détails qui mettent en évidence ce qui le rend unique.</p><div class="form-group mb-3">
  <%= f.label :titre %>
  <%= f.text_field :title, autofocus: true, autocomplete: "title",
              class: 'form-control text-success', require: true
      %>
</div>
<div class="form-row mb-2">
  <div class="col">
      <%= f.label :organisateur %>
      <div>
      <% if !@organizers.nil? %>
        <%= f.select :organizer_id, options_for_select(@organizers.collect{|o| [o.name, o.id]}, params[:organizer_id]),
                  {}, class: "form-control text-success"
          %>
      <% end %>

      </div>
  </div>
  <div class="col">
    <%= f.label :prix%>
    <%= f.number_field :price, class:"form-control text-danger", value: params[:price] %>
  </div>

</div>
<div class="mb-4">
    <span><small>
Ce profil décrit un organisateur unique et affiche tous les événements sur une seule page.        <a href="#">
        Afficher les informations sur l'organisateur</a>
      </small></span>
  </div>

<div class="form-row mb-4">
  <div class="col">
    <%= f.label :type_id %>
    <% if !@types.nil? %>
    <div class="">
      <%= f.select :type_id, options_for_select(@types.collect{|o| [o.title, o.id]}),
                  {}, class: "form-control text-success"
          %>
    </div>
    <% end %>
  </div>
  <div class="col">
    <%= f.label :categorie %>
    <% if !@categories.nil? %>
    <div>
      <%= f.select :category_id, options_for_select(@categories.collect{|o| [o.title, o.id]}),
                  {}, class: "form-control text-success"
          %>
    </div>
    <% end %>
  </div>
</div>

<div class="form-group mb-5">
  <%= f.label :tags %><br>
<small>Améliorez la visibilité de votre événement en ajoutant des balises pertinentes au sujet.</small>  <input class="form-control text-success mt-2">
  <div>
    <small>0/10 tags</small>
  </div>

</div>
<hr>

<div class="form-group mb-3">
  <h3>Lieu</h3>
<span>Aidez les gens de la région à découvrir votre événement et indiquez aux participants où se présenter.</span></div>
<div class="form-row mb-3">
  <div class="col">
    <%= f.label :type_location %>
    <%= f.select :type_location, options_for_select(["Avenue", "Online event", "To be announced"]), {}, 
              class: 'form-control text-success mb-3'
    %>
  </div>
   <div class="col">
        <%= f.label :quantite_maximal%>
        <%= f.text_field :max_quantity, class:"form-control text-success", value: params[:max_quantity] %>
        <small>Nombre illimité ou exact.</small>
  </div>
</div>
   
<div class="form-group mb-5">
  <%= f.label :location %>
  <%= f.text_field :location, class: 'form-control text-success'%>
  <div class="mt-1">
  <small>Vous pouvez ignorer ce champ si vous sélectionnez "Événement en ligne" ou "À annoncer"

.</small>
  </div>
</div>
<hr>

<div class="form-group mb-3">
  <h3>Date Et Heure</h3>
  <span>Informez les spectateurs du début et de la fin de votre événement afin qu'ils puissent planifier leur participation.</span>
</div>

<div class="form-row mb-2">
  <div class="col">
    <%= f.label :Date_Debut %>
    <%=  f.text_field :start_date, id: 'startDate', class: "form-control dpicker text-success", require: true %>
    
  </div>
  <div class="col">
    <%= f.label :Debut %>
     <%=  f.text_field :start_at, id: 'startAt',class: "form-control tpicker text-info", require: true %>
  </div>
</div>

<div class="form-row mb-5">
  <div class="col">
    <%= f.label :Date_Fin %>
    <%=  f.text_field :end_date, id: 'startDate', class: "form-control dpicker text-success", require: true %>
    
  </div>
  <div class="col">
    <%= f.label :Fin %>
     <%=  f.text_field :end_at, id: 'endAt',class: "form-control tpicker text-info", require: true %>
  </div>
</div>

<hr>

<div class="form-group mb-5">
  <h3>Affiche Principal de Votre Evènement</h3>
  <span>Il s'agit de l'image que les participants verront en haut de votre annonce. Utilisez une image de haute qualité :  2160x1080px
    (2:1 ratio)</span>Le premier
  <section class="drag-drop-phot">
    <%# <form action="" method="POST" enctype="multipart/form-data"> %>

      <div class="form-group">
        <div class="preview-zone hidden">
          <div class="box box-solid">
            <div class="box-header with-border">
              <div><b>Previsualisez</b></div>
              <div class="box-tools pull-right">
                <button type="button" class="btn btn-danger btn-xs remove-preview">
                  <i class="fa fa-times"></i> Supprimez ce formulaire
                </button>
              </div>
            </div>
            <div class="box-body"></div>
          </div>
        </div>
        <div class="dropzone-wrapper--cus">
          <div class="dropzone-desc">
            <i class="glyphicon glyphicon-download-alt"></i>
            <p>Choisissez une affiche ou glissez deposez.</p>
          </div>
          <input type="file" name="img_logo" class="dropzone">
        </div>
      </div>

      <button type="submit" class="btn btn-primary ml-0">Télécharger</button>
    <%# </form> %>
  </section>

</div>
<hr>

<div class="form-group mb-3">
  <h3>Description</h3>
  <span>Ajoutez plus de détails à votre événement, comme votre emploi du temps, vos sponsors ou vos invités vedettes.</span>
  <%= f.text_area :description, class: "form-control text-success" ,rows: "15" %>
</div>
<div class="form-check mb-5">
  <input type="checkbox" class="form-check-input" id="ispublish">
  <label class="form-check-label" for ="ispublish">Is publish?</label>
</div>


<div class="actions mb-5">
  <%= f.submit "Save" , class:"btn btn-danger mr-3"%>
  <%= link_to 'Back', :back , class: 'btn btn-dark ml-auto' %>
</div>
<% end %>

