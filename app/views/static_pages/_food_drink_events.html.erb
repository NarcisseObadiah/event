 <div class="tab-pane fade" 
 id="food_drink_events" role="tabpanel" aria-labelledby="food_drink_events-tab">
 
 <% if @food_drink_events.nil? || @food_drink_events.count < 1 %>
    <div class="mt-4">
        Pas d'Evènement
    </div>
  <% else %>
  <%= render partial: 'card_event', locals: {events: @food_drink_events} %>
 <% end %>
 
 </div>