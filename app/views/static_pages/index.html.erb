<!DOCTYPE html>
<html>

<head>
  <% content_for :title, "Home" %>
</head>

<body>

  <!-- Overlay section-->
  <section id="intro-section mb-5">
    <div class="container-fluid">
      <div class="row">
        <div class="col-12 col-md-12 col-lg-10 offset-lg-1 intro-wrapper">

          <!-- Intro info -->
          <div class="intro-send-message p-3 text-white mb-3">
            <h2>Le temps presse</h2>
            <span>Rejoignez le mouvement pour faire vivre les lieux indépendants. <a
                href="https://www.nivassoc.org/take-action">Envoyez un message au Congrès pour</a> #SaveOurStages.</span>
          </div>
          <div class="intro-photo">
              <div class="intro-fight p-3 text-center mx-auto">
                  <span class="intro-fight-title px-0 mt-3">Lutte pour l'égalité raciale</span><br>
                  <p class="mt-3">Les gens se mobilisent dans tout le pays. Trouvez la manifestation la plus proche ou <a href="<%= new_event_path %>">commencez le votre</a> maintenant.<span>
              </div>
          </div>
          <!--End intro info -->

          <%= render 'top_categories'%>

          <!--Popular event -->
          <div class="event-list">
            <h1 class="mb-2">Événements en ligne populaires</h1>
            <ul class="nav nav-tabs" id="myTab" role="tablist">
              <li class="nav-item">
                <a class="nav-link active" id="all_events-tab" data-toggle="tab" href="#all_events" role="tab" aria-controls="all_events" aria-selected="true">Tous les Evenements</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id="today_events-tab" data-toggle="tab" href="#today_events" role="tab" aria-controls="today_events" aria-selected="false">Aujourd'hui</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id="weekend_events-tab" data-toggle="tab" href="#weekend_events" role="tab" aria-controls="weekend_events" aria-selected="false">Weekend</a>
              </li>
               <li class="nav-item">
                <a class="nav-link" id="free_events-tab" data-toggle="tab" href="#free_events" role="tab" aria-controls="free_events" aria-selected="false">Gratuit</a>
              </li>
               <li class="nav-item">
                <a class="nav-link" id="music_events-tab" data-toggle="tab" href="#music_events" role="tab" aria-controls="music_events" aria-selected="false">Music</a>
              </li>
               <li class="nav-item">
                <a class="nav-link" id="food_drink_events-tab" data-toggle="tab" href="#food_drink_events" role="tab" aria-controls="food_drink_events" aria-selected="false">Nourriture & Boisson</a>
              </li>
               <li class="nav-item">
                <a class="nav-link" id="charity_events-tab" data-toggle="tab" href="#charity_events" role="tab" aria-controls="charity_events" aria-selected="false"></a>
              </li>
            </ul>
            <div class="tab-content" id="myTabContent">
              <%= render 'all_events'%>
              <%= render 'today_events'%>
              <%= render 'weekend_events'%>
              <%= render 'free_events'%>
              <%= render 'music_events'%>
              <%= render 'food_drink_events'%>
              <%= render 'charity_events'%>  
            </div>
          </div>

        </div>
      </div>
    </div>
  </section>
</body>

</html>