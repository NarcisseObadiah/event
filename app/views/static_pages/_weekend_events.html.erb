 <div class="tab-pane fade" 
 id="weekend_events" role="tabpanel" aria-labelledby="weekend_events-tab">
 
 <% if @weekend_events.nil? ||  @weekend_events.count < 1 %>
    <div class="mt-4">
    Pas d'Evènement
    </div>
  <% else %>
    <%= render partial: 'card_event', locals: {events: @weekend_events} %>
 <% end %>
 
 </div>